'use strict';

const _ = require('underscore');

const utils = require('../../utils');
const Loss = utils.Loss;

module.exports = (token, fields) => {
  console.log('checking loss');
  console.log(token);
  console.log(fields);
};
